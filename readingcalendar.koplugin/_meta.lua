local _ = require("gettext")
return {
    name        = "readingcalendar",
    fullname    = _("Reading Calendar"),
    description = _([[Month calendar for the SimpleUI homescreen showing the covers of the books read each day. Requires the SimpleUI plugin.]]),
    version     = "1.0.0",
}
